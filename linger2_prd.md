# Linger 2.5 — 产品需求文档 (PRD)

> **版本:** 2.5  
> **状态:** 开发中  
> **最近更新:** 2026-08-06  
> **平台:** macOS 13+  
> **技术栈:** AppKit（原生菜单栏应用）  
> **核心哲学:** "一拉即走，松手计时"

---

## 1. 产品概述

### 1.1 核心理念

Linger 是一个 macOS 菜单栏计时器应用，核心交互是 **从菜单栏图标向下拖拽来设定倒计时**。拖拽长度 = 计时时长，松手即开始计时。

### 1.2 目标用户

- 需要快速计时的工作者（专注时间管理、番茄钟）
- 需要将计时结果同步到日历的用户
- 追求精致 macOS 原生体验的用户

### 1.3 设计语言

- 原生 AppKit，不依赖 SwiftUI
- 毛玻璃（Glassmorphism）质感
- 暗黑/亮色主题自适应
- 琥珀金（Amber）作为强调色
- 最小化界面，信息密度适中

---

## 2. 功能架构图

```
Linger 2.0
├── 菜单栏系统（MenuBarManager）
│   ├── 状态栏图标（动态图标 + 倒计时文字）✅
│   ├── 左键交互（拖拽开始计时）✅
│   ├── 右键交互（上下文菜单）✅
│   ├── 鼠标悬停（计时列表弹窗 HoverListView）✅
│   └── 拖拽反馈窗口（DragFeedbackView）✅
│
├── 计时引擎（TimerManager）
│   ├── TimerEntry（单个计时实体）✅
│   ├── 即时倒计时（秒级精度）✅
│   ├── 预约计时（指定未来时间开始）✅（内联展开、到点自动激活）
│   ├── 暂停/继续 ✅（HoverListView UI 完整）
│   ├── 最多 10 个并发计时 ✅
│   ├── 持久化（JSON 存 Application Support）✅
│   └── 定时清理（未记录的历史条目）✅（每周/每月清理 + 记录导出前自动归档）
│
├── 完成弹窗（CompletionBannerManager）
│   ├── 自绘玻璃横幅（替换系统通知 + 无需通知权限）✅
│   ├── 标题行：Ring 图标 + Linger + ✓ 绿 + 时间 + ✕ 关闭 ✅
│   ├── 内容行：日程模块（显示/内联输入）+ mm:ss 倒计时 + ↻ 重复 + ✓ 确认 ✅
│   ├── 8s 无交互自动淡出；多条右上角堆叠 ✅
│   ├── 日程编辑时暂停 8s 自动消失（防丢输入）✅
│   └── 设置开关「完成弹窗（强提醒）」；关闭后仅保留菜单栏倒计时与提示音 ✅
│
├── 日历集成（CalendarManager + CalendarRecorder）✅
│
├── 设置窗口（SettingsWindow）✅（5 tab + section/row 范式 + 关于票据面板）
│
├── 关于窗口 → 集成到设置 tab 5（AboutTicketView 白底票据面板）✅
│
├── 记录导出（RecordExporter）✅（Markdown 按天归档到 ~/Documents/Linger 计时记录.md）
│
├── 右键菜单
│   ├── 设置… / 系统授权（已授权→灰显"已获取授权"）/ 退出⌘Q ✅
│   └── autoenablesItems=false 修复授权菜单项灰显 bug ✅
│
├── FLIP 动画系统（HoverListView 内建）
│   ├── 卡片位移动画 ✅（cubic-bezier 0.5s）
│   ├── 窗口高度同步动画 ✅
│   └── 分隔线过渡动画 ✅
```

---

## 3. 详细功能需求

### 3.1 菜单栏系统

#### 3.1.1 状态栏图标

| 需求 | 描述 | 状态 |
|:-----|------|------|
| 图标位置 | macOS 菜单栏右侧（NSStatusItem） | ✅ |
| 默认图标 | "Ring" 风格（环形图标），自绘 template image | ✅ |
| 图标切换 | 用户已决定只保留 Ring（唯一菜单栏图标）；设置三选一已删 | 🚫 已砍 |
| 计时状态显示 | 有计时运行时，图标右侧显示最近结束条目的倒计时文字 | ✅ |
| 时间格式 | HH:MM:SS / HH:MM / MM:SS 三档可选 | ✅ |

#### 3.1.2 左键交互 — 拖拽计时

| 需求 | 描述 | 状态 |
|------|------|------|
| 触发 | 左键点击状态栏按钮 → 启动拖拽状态机 | ✅ |
| 反馈 | 从图标位置向下拖拽，显示一段琥珀金发光竖线 | ✅ |
| 时长映射 | 拖拽距离 → 计时时长（非线性：s = d² 曲线） | ✅ |
| 范围 | 最少 10 秒，最大由用户设定（默认 30 分钟，上限 24 小时） | ✅ |
| 吸附 | 接近整分钟时自动吸附（吸附阈值 5 秒） | ✅ |
| 双轨显示 | 拖拽时同时显示"倒计时"和"结束时间"两个读数 | ✅ |
| 悬停切换 | 鼠标移到左侧：倒计时读数高亮；移到右侧：结束时间高亮 | ✅ |
| 模式切换 | 三种双轨模式：倒计时+结束时间 / 仅倒计时 / 仅结束时间 | 🚫 待定（linger_dualRailMode token 已定义，设置 UI 和逻辑未做）|
| 拖拽溢出 | 拖拽超出最大距离时 → 回弹动画 → 自动开始最长计时 | ⏳ 弹簧反馈已实现，自动开始最长计时未实现 |
| 快速标题 | 拖拽时按住 Fn/Ctrl/Opt 键，自动使用预设标题 | ✅ |
| Command 取消 | 拖拽中按 Command 键取消 | ✅ |

#### 3.1.3 右键交互 — 上下文菜单

| 需求 | 描述 |
|------|------|
| 菜单项 | 关于 Linger / 设置 / 分隔线 / 退出 |
| 快捷键 | 退出 = ⌘Q |

#### 3.1.4 鼠标悬停 — 计时列表

| 需求 | 描述 |
|------|------|
| 触发 | 鼠标移到状态栏图标上 → 弹出计时列表浮窗 |
| 浮窗布局 | 圆角毛玻璃面板，菜单栏下方弹出 |
| 三组排序 | 计时中（剩余时间升序）→ 分隔线 → 暂停（剩余时间升序）→ 分隔线 → 未开始（剩余时间升序） |
| 颜色方案 | 计时中=琥珀金 bold、暂停=灰色 bold、未开始=灰色 light |
| 分隔线 | 灰色半透明（仅组间存在条目时显示），配合 FLIP 动画过渡 |
| 暂停/继续 | 每条右侧有暂停/继续按钮（pause/play SF Symbol），点击切换 |
| 停止 | 每条右侧有停止按钮（stop SF Symbol） |
| 全部暂停/继续 | 底栏右侧按钮，根据是否有 running 动态切换文字和图标 |
| 预约计时 | 底栏日历图标按钮，点击浮窗伸缩延长、内联展开预约编辑区（见 §3.1.6） |
| 点击编辑 | 单击卡片进入标题编辑（NSTextField），Enter 提交 / ESC 取消 |
| 首次锁定 | 已编辑标题不可再次修改（为日历同步一致性） |
| 标题编辑位 | 输入框与绘制后的文字位置一致，无跳位 |
| 默认标题 | 无标题时行留空，不显示任何字符 |
| 卡片动画 | FLIP 动画系统：暂停/新增/移除时卡片平滑位移，窗口高度同步伸缩（0.5s cubic-bezier） |
| 自动隐藏 | 鼠标离开 0.15 秒后淡出 |

#### 3.1.5 拖拽反馈窗口（DragFeedbackView）

| 需求 | 描述 | 状态 |
|------|------|------|
| 外观 | 透明背景，仅显示琥珀金渐变竖线 + 圆点 + 双轨时间标签 | ✅ |
| 位置 | 状态栏图标正下方，起点贴近图标（topY=12） | ✅ |
| 竖线渐变 | 上深铜→下亮金渐变，n⁴曲线保持全程可见，max 时统一亮金色 | ✅ |
| 弹簧线宽 | 超出 max 后微微变细（~3pt→~2.5pt），肉眼难辨 | ✅ |
| 双轨标签 | 水平排列：左 `for` + 倒计时时长 / 右 `til` + 结束时刻，中间细分隔线 | ✅ |
| 字号 | for/til 标签 13pt；两轨数字均 24pt 等大，水平底部对齐 | ✅ |
| 浅色模式 | 系统自适应颜色 + NSShadow 兜底，深浅模式均可读 | ✅ |
| 悬停切换 | 鼠标在左半边→for 亮起琥珀金(opacity 1)、til 变暗(opacity 0.3)；反之亦然；0.35s cubic-bezier 平滑过渡 | ✅ |
| 双边吸附 | 左列 hover→时长取整；右列 hover→完成时间取整 | ✅ |
| 窗口宽度 | 280pt，容纳完整双轨文字不裁切 | ✅ |
| 吸附反馈 | 时长/完成时间接近整分钟自动修正 | ✅ |

#### 3.1.6 预约计时浮窗（ScheduleTimerView）

| 需求 | 描述 |
|------|------|
| 触发 | hover-list 底栏日历图标点击 → 浮窗伸缩延长，内联展开预约编辑区（不弹出独立窗口） |
| 外观 | hover-list 原浮窗向下延长（300pt 宽），毛玻璃圆角，编辑区直接嵌入列表下方 |
| 日期 | calendar 图标 + 文字，紧凑同行胶囊容器，显示"8月3日 周一" |
| 时间 | clock 图标 + 等宽数字，紧凑同行胶囊容器，显示 HH:MM |
| 倒计时时长 | timer 图标 + 数字 + 单位，紧凑同行胶囊容器 |
| 日程名称 | pen-line 图标 + 输入框，紧凑同行胶囊容器，与 hover-list 键入逻辑一致 |
| 布局 | 日期+时间 同行，时长+名称 同行，预计结束+图标按钮同行（共3行，无标题行） |
| 预计结束 | 自动计算 = 开始时间 + 时长，arrow-right 图标 + 灰色等宽文字 |
| 确认预约 | check 图标圆形按钮（琥珀金填充），点击后创建 scheduled TimerEntry |
| 取消 | x 图标圆形按钮（灰色），收起编辑区不创建计时 |
| 关闭 | 再次点击日历图标 / 确认 / 取消，浮窗收缩回计时列表态 |
| 无窗口唤醒 | 浮窗内完成所有输入，不唤醒主应用窗口，不弹出独立窗口 |

> **数据流：** 确认预约 → 创建 TimerEntry（isScheduled=true, scheduledStartTime, scheduledEndTime, duration）→ 到点自动激活为 running → 倒计时归零 → 通知横幅

### 3.2 计时引擎

#### 3.2.1 计时实体（TimerEntry）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 唯一标识 |
| duration | TimeInterval | 总计时长 |
| remainingTime | TimeInterval | 剩余时间 |
| isRunning | Bool | 运行中 |
| isPaused | Bool | 已暂停 |
| isScheduled | Bool | 预约计时 |
| originalStartTime | Date | 实际开始时间 |
| originalEndTime | Date | 实际/预计结束时间 |
| scheduledStartTime | Date? | 预约开始时间 |
| scheduledEndTime | Date? | 预约结束时间 |
| scheduledTitle | String? | 预约标题 |
| hasRecorded | Bool | 是否已记录到日历 |
| predefinedTitle | String? | 预设标题（快捷键） |
| calendarEventId | String? | 日历事件 ID |

#### 3.2.2 核心行为

| 需求 | 描述 |
|------|------|
| 即时计时 | 拖拽松手 → 立即开始倒计时 |
| 预约计时 | 设定未来时间 → 到点自动开始计时 | ✅（内联展开编辑 + 到点激活 + 日历事件同步）|
| 暂停/继续 | 支持单条暂停及继续 |
| 批量暂停 | "暂停全部" / "继续全部" |
| 停止 | 单条停止并移除 |
| 清空全部 | 停止所有计时并清空列表 |
| 上限 | 同时最多 10 个计时 |
| 精度 | 每秒 tick 更新 |
| 状态变更通知 | 任何状态变化 → 通过 NotificationCenter 广播 |
| 标签标题 | 持有所见即所得的可读文本标签 |
| 停留确认 | 暂停状态的确认和保护机制 |

#### 3.2.3 持久化

| 需求 | 描述 |
|------|------|
| 存储位置 | `~/Library/Application Support/Linger/timers.json` |
| 存储时机 | 每次状态变化后异步保存 |
| 恢复时机 | 应用启动时自动恢复所有计时 |
| 恢复逻辑 | 暂停条目保持暂停，运行中条目校正剩余时间 |
| 清理策略 | 已完成的已记录条目按用户设定的间隔清理（每周/每月/从不） |

### 3.3 浮窗系统（CountdownPillPanel）

> **⚠️ 已废弃（2026-08-02）：** v2.0 原型阶段已移除倒计时浮窗页面。计时完成改由 macOS 原生通知横幅承接（见 §3.4）。旧代码 CountdownPillPanel.swift / CountdownFloater.swift 保留待清理。以下规格仅作历史记录保留。

> **历史说明：** v2.0 曾将旧的多独立浮窗（CountdownFloater）合并为单窗口统一面板 CountdownPillPanel。旧 CountdownFloater 代码保留但不再使用。

#### 3.3.1 统一倒计时提醒面板

| 需求 | 描述 | 状态 |
|------|------|------|
| 触发 | 计时剩余时间 <= leadTime（默认 60 秒） | ✅ |
| 外观 | 居中屏幕，暗色毛玻璃圆角面板，琥珀金呼吸边框 | ✅ |
| 布局 | 单窗口多行，无标题栏/分隔线/控制按钮 | ✅ |
| 时间呼吸 | 1 秒周期，opacity 1.0↔0.6，多行同相 | ✅ |
| 边框呼吸 | 1 秒周期，opacity 0.40↔0.15，仅在有 running 时启动，与数字同相 | ✅ |
| 排序 | running 组在上（剩余时间升序）+ zero 组在下 | ✅ |
| 分组分隔线 | 仅两组同时存在时显示 1px 半透明分隔线 | ✅ |
| 默认标题 | 未编辑的行显示 `-`（灰色）替代"未命名" | ✅ |
| 标题编辑 | 选中行 → NSTextField 可编辑 → Enter 提交 / ESC 取消 | ✅ |
| 编辑提示 | 编辑时标题右侧显示 `return` + arrow.turn.down.left | ✅ |
| 确认图标 | Enter 后显示 `done` + checkmark.circle.fill 永久保留 | ✅ |
| tab 提示 | 未选中行右侧显示 `tab` + arrow.right.to.line | ✅ |
| 编辑文字色 | 编辑中白色，失焦恢复灰色 | ✅ |
| 关闭按钮 | 面板右上角，压在边框线上，白色圆环，hover 变亮 | ✅ |
| 关闭动画 | 0.3s shootOut 飘向菜单栏 + 淡出，无矩形阴影框 | ✅ |
| 关闭方式 | 手动关闭（X / ESC），不自动消失 | ✅ |
| 关闭闪现 | 关闭动画期间行保持原样，不闪回 00:00 / - | ✅ |
| 防反弹 | 已关闭条目不重新显示（dismissedEntryIDs） | ✅ |
| 拖动 | 面板空白区 + 行本身均可拖动窗口 | ✅ |
| 排序动画 | 行位置变化时 0.35s cubic-bezier 过渡 | ✅ |
| 条目增删 | 新行淡入，移除行立即消失 | ✅ |
| Tab 切换 | 每按一次 Tab 切换一行（循环），titleField 拦截已修复 | ✅ |

#### 3.3.2 淘汰模块

| 模块 | 状态 | 说明 |
|------|------|------|
| SmallCountdownView（旧） | ❌ 已废弃 | 被 CountdownPillPanel 替代 |
| FinalCountdownView（旧） | ❌ 已废弃 | 功能合并到 CountdownPillPanel |
| CountdownFloater.swift | 🚫 已删 | 文件已在代码库中删除 |

#### 3.3.3 Toast 消息

| 需求 | 描述 |
|------|------|
| 用途 | 非侵入式提示（已达上限 / 拖拽提示等） |
| 外观 | 居中毛玻璃圆角面板 |
| 动画 | 淡入（0.4s）→ 停留（2.5s）→ 淡出 |
| 位置 | 屏幕中央 |

### 3.4 完成弹窗 / 通知系统

#### 3.4.1 权限管理

| 需求 | 描述 |
|------|------|
| 首次启动 | 自动请求通知授权 |
| 状态检查 | 每次使用前检查权限状态 |
| 拒绝处理 | 权限被拒绝时回退到本地 NSSound 播放提示音 |

#### 3.4.2 通知内容

| 需求 | 描述 |
|------|------|
| 完成态标识 | 无"计时完成"标题，用 ✓ circle-check 图标表示完成态 |
| 模块化布局 | 日程模块 + 记录时间模块 + 重复按钮 + 确认按钮，整合到同一行 |
| 日程模块 | 有内容时直接显示名称（如"写代码"）；无内容时为输入态（铅笔图标 + placeholder=日程，参考 hover-list 键入逻辑） |
| 记录时间模块 | 直接取用倒计时时间，mm:ss 格式（如 25:00），琥珀金等宽数字 |
| 横幅可关闭 | 用户可手动关闭横幅通知，不强制交互 |
| 通知分类 | dragTimer 类别（含 Repeat + Confirm Action）/ scheduledTimer 类别（无 Action） |
| 无窗口唤醒 | 整个通知交互流程（输入/确认/重复）均在通知横幅内完成，不唤醒任何应用窗口 |

#### 3.4.3 通知 Action

| Action | 图标 | 说明 |
|--------|------|------|
| 重复（Repeat） | rotate-ccw | 以同样时长再开始一个计时 |
| 确认（Confirm） | check | 日程有内容→写入日历/关闭通知；日程无内容→提交内联输入内容并写入日历 |

> **设计说明：** 旧版"记录（Record）"Action（UNTextInputNotificationAction）和"默认点击弹出对话框"已移除。日程无内容时直接在通知横幅内通过日程模块的输入态完成命名，无需弹出任何额外窗口。两个 Action 均以纯图标按钮呈现，无文字标签。

#### 3.4.4 提示音

| 需求 | 描述 |
|------|------|
| 开关 | 设置中可关闭提示音 |
| 音效选择 | 支持 macOS 内置系统铃声音效（Ping / Basso / Blow / Bottle / Frog / Funk 等） |

### 3.5 日历集成

#### 3.5.1 权限管理

| 需求 | 描述 |
|------|------|
| 按需授权 | 首次尝试写入日历时请求权限 |
| 状态显示 | 设置/关于窗口显示授权状态 |
| 跳转设置 | 未授权时提供"前往系统设置"链接 |

#### 3.5.2 日历管理

| 需求 | 描述 |
|------|------|
| 专用日历 | 自动创建/使用名为"Linger"的日历 |
| 日历切换 | 设置中可选择系统其他日历 |
| 历史兼容 | 自动迁移旧版日历名称（🔔Linger / DragTimer 等） |

#### 3.5.3 写入规则

| 需求 | 描述 |
|------|------|
| 写入模式 | 手动（仅拖拽时快捷键触发）/ 自动（使用默认标题）/ 每次询问 |
| 默认标题 | 设置中可配置默认活动标题 |
| 最短时长 | 不满 5 分钟的计时自动补齐到 5 分钟 |
| 写入时机 | 计时完成 + 模式判断 → 异步创建日历事件 |

#### 3.5.4 快捷预设标题

| 快捷键 | 用途 |
|--------|------|
| Fn + 拖拽 | 使用 Fn 预设标题（如"开会"） |
| Ctrl + 拖拽 | 使用 Ctrl 预设标题（如"写代码"） |
| Opt + 拖拽 | 使用 Opt 预设标题（如"休息"） |
| 设置配置 | 三个快捷键各自绑定可编辑的标题字符串，支持留空（不激活） |

### 3.6 设置窗口

#### 3.6.1 窗口结构

| 属性 | 值 |
|------|-----|
| 尺寸 | 宽 520pt，高度自适应（跨 Tab 切换时平滑 resize 到当前内容高度，0.4s cubic-bezier） |
| 外观 | 顶部图标 Tab 栏布局（操作 / 通知 / 日历 / 通用），毛玻璃背景 |
| 标题栏 | 38pt 高，居中显示当前面板名称，左侧交通灯（仅关闭按钮可用，最小化/缩放隐藏） |
| Tab 栏 | 图标 + 文字标签，激活项琥珀金高亮背景，居中排列 |
| 样式 | Tab 内容区直接平铺控件，无 NSBox 卡片分组 |

#### 3.6.2 面板 0 — 操作设置

| 控件 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 下拉线最大长度 | Slider | 50% | 范围 25%~75%，影响拖拽灵敏度 |
| 最大计时时长 | 数字输入框 | 30（分钟） | 上限 1440（24h），下限 5 |
| 双轨显示 | 下拉菜单 | 倒计时+结束时间 | 选项：both / countdown / endTime |
| 时间格式 | 下拉菜单 | HH:MM:SS | 选项：hms / hm / ms |

#### 3.6.3 面板 1 — 通知

| 卡片 | 控件 | 说明 |
|------|------|------|
| 通知授权 | 状态文本 + 按钮 | 显示已授权/未授权，点击跳转系统设置 |
| 计时完成 | 开关：计时完成时通知 | 默认开启 |
|  | 开关：播放提示音 | 默认开启，关联下方音效选择 |
|  | 下拉菜单：提示音选择 | 关闭提示音时禁用 |
| ~~倒计时浮窗~~ | ~~开关：显示倒计时浮窗~~ | ⚠️ 已废弃（2026-08-02），计时完成改由通知横幅承接 |

#### 3.6.4 面板 2 — 日历

| 卡片 | 控件 | 说明 |
|------|------|------|
| 日历授权 | 状态文本 + 按钮 | 显示已授权/未授权，点击跳转系统设置 |
| 写入设置 | 下拉菜单：目标日历 | 列出所有系统日历 |
|  | 下拉菜单：写入方式 | 每次询问 / 自动 / 手动 |
|  | 输入框：默认标题 | 仅在自动模式下可用 |
|  | 快捷键预设区域 | Fn / Ctrl / Opt 三个输入框，各自绑定标题 |

#### 3.6.5 面板 3 — 通用

| 控件 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 开机自启 | 开关 | 关 | 通过 SMAppService 注册启动项 |
| 自动清理 | 下拉菜单 | 每周 | 每周 / 每月 / 从不 |
| 菜单栏图标 | 下拉菜单 | Ring | Ring / Classic / timer SF Symbol |

### 3.7 关于窗口

#### 3.7.1 窗口结构

| 属性 | 值 |
|------|-----|
| 尺寸 | 480 x 320 |
| 外观 | 居中布局，无 emoji，毛玻璃背景 |
| 窗口控制 | 恢复标准关闭按钮，隐藏最小化和缩放 |
| Level | .normal（非 floating） |

#### 3.7.2 内容（从上到下居中排列）

| 元素 | 规格 |
|------|------|
| 应用图标 | 64×64，居中，从 AppIcon 资源加载 |
| 应用名 "Linger" | 24pt Bold，labelColor |
| 版本号 "版本 x.x" | 12pt Regular，secondaryLabelColor |
| 标语 "一拉即走，松手计时" | 11pt Italic，tertiaryLabelColor |
| 权限卡片行 | 两张并排卡片（日历 + 通知） |

#### 3.7.3 权限卡片

每张卡片包含：
- SF Symbol 图标（20pt，accentColor）
- 状态文字："已授权" / "未授权"
- 操作按钮：已授权→"管理…" / 未授权→"开启"
- 每 3 秒自动刷新权限状态

---

## 4. 非功能需求

### 4.1 性能

| 指标 | 目标 |
|------|------|
| 应用启动时间 | < 1 秒（菜单栏图标出现） |
| 拖拽帧率 | 30fps（Timer 轮询） |
| 动画帧率 | 60fps（AppKit 原生动画） |
| 内存占用 | < 50MB 常驻 |
| CPU 占用 | 空闲 < 1%，计时中 < 3% |

### 4.2 兼容性

| 要求 | 说明 |
|------|------|
| 最低系统 | macOS 13 Ventura |
| 架构 | Apple Silicon + Intel |
| 暗黑模式 | 完全支持 |
| 多显示器 | 浮窗位置正确处理 |
| Mission Control | 窗口位置正确保留 |
| 系统语言 | 中文 + 英文 |

### 4.3 安全性

| 要求 | 说明 |
|------|------|
| 沙箱 | 不需要沙箱（菜单栏应用 + 日历访问） |
| 权限 | 仅在需要时请求（通知 / 日历） |
| 数据存储 | 仅本地，不联网，不上传任何数据 |
| 崩溃恢复 | 计时状态持久化，重启后自动恢复 |

---

## 5. 技术架构

### 5.1 模块划分

| 模块 | 文件 | 职责 | 依赖关系 |
|------|------|------|----------|
| AppDelegate | AppEntry.swift | 启动流程、环境检测、激活策略 | 无 |
| MenuBarManager | MenuBarManager.swift | 状态栏、拖拽、悬停、浮窗控制 | TimerManager |
| TimerManager | TimerManager.swift | 计时引擎、持久化 | 无 |
| CountdownPillPanel | CountdownPillPanel.swift | ~~统一倒计时提醒面板~~ ⚠️ 已废弃 | MenuBarManager（回调） |
| HoverListView | HoverListView.swift | 悬停计时列表 | TimerManager |
| ScheduleTimerView | (独立文件) | 预约计时浮窗 | TimerManager |
| TimerEntry | TimerEntry.swift | 计时实体定义 | 无 |
| SettingsWindow | (独立文件) | 设置窗口 | TimerManager, CalendarManager |
| AboutWindow | (独立文件) | 关于窗口 | CalendarManager |
| CalendarManager | (独立文件) | 日历集成 | 无 |
| NotificationManager | (独立文件) | 通知管理 | 无 |

### 5.2 状态机

#### 拖拽状态机

```
idle → pressed（点击按钮）
     → dragging（启动 timer 轮询 + monitor 监听）
         → leftMouseUp → released → finish / cancel
         → command 按下 → cancel → idle
     → timer 到期 → released → idle
```

#### 计时器状态机

```
init → running → paused → running
                → finished → 记录 / 忽略 → 移除
     → scheduled → waiting → running（到点激活）
```

#### 倒计时浮窗状态机（CountdownPillPanel）

```
面板隐藏 → 有条目进入 leadTime → 面板显示
     → 手动关闭（X/ESC）→ isUserDismissed → 面板隐藏
         → 已 dismiss 条目不重建 → 新条目进入 leadTime → 面板显示

行状态: running → zero → 留在面板底部（灰色）
编辑状态: 未选中 → 选中（标题可编辑）→ Enter 提交 / ESC 取消
```

### 5.3 数据流

```
用户操作 (拖拽/点击/键盘)
    ↓
MenuBarController 处理
    ↓
TimerManager 更新计时状态
    ↓
NotificationCenter 广播 timerStateChanged
    ↓
MenuBarController 更新：菜单栏 / 浮窗 / 悬停窗
CalendarManager 保存事件（条件触发）
TimerManager.saveToDisk() 异步持久化
```

### 5.4 关键设计决策

| 决策 | 理由 |
|------|------|
| AppKit 而非 SwiftUI | 菜单栏应用需要 NSStatusItem 及系统级交互，SwiftUI 控制力不足 |
| 编译时宏区分 Debug/Release | 避免触发 linkd 主线程阻塞 |
| NSEvent.addLocalMonitorForEvents 仅用于拖拽 | 状态栏按钮点击走 target/action，不经过 monitor |
| NSTrackingArea owner 为 NSView 子类 | owner 必须是 NSView 子类，否则崩溃 |
| 拖拽用 timer 轮询而非 while 循环 | 避免主线程阻塞 |
| 所有 UI 视图在 OverlayView.swift | 现状，2.0 建议按视图拆分为独立文件 |

---

## 6. 变更清单 — 从 1.0 到 2.0

### 6.1 代码重构

| 项目 | 1.0 状态 | 2.0 目标 |
|------|----------|----------|
| 文件拆分 | 4 个文件，OverlayView.swift 超长 | 按视图拆分为独立文件 |
| SettingsWindow | OverlayView.swift 内联 | 独立文件 |
| AboutWindow | OverlayView.swift 内联 | 独立文件 |
| CalendarManager | OverlayView.swift 内联 | 独立文件 |
| NotificationManager | OverlayView.swift 内联 | 独立文件 |
| HoverListView | OverlayView.swift 内联 | 独立文件 |
| 激活策略 | Runtime sysctl 检测 | 保留，已验证可靠 |
| 事件处理 | 已遵循铁律 | 保留，验证无 monitor 滥用 |

### 6.2 UI 升级

| 项目 | 1.0 状态 | 2.0 目标 |
|------|----------|----------|
| 设置窗口 | 已按卡片式重写 | 保留设计，修复潜在问题 |
| 关于窗口 | 已按居中布局重写 | 保留设计，修复潜在问题 |
| 菜单栏菜单 | 测试 + 关于 + 设置 + 退出 | 根据需求决定是否丰富 |
| 浮窗动画 | 基本动画已实现 | 优化动画曲线 + 防重入 |

### 6.3 已知问题修复

| 问题 | 优先级 | 说明 |
|------|--------|------|
| NSTrackingArea owner 问题 | P0 | 1.0 中使用 `self` (MenuBarController) 作为 owner，违反了铁律，必须改为使用 statusItem.button |
| 设置窗口控制按钮 | P1 | 还原标准关闭按钮，隐藏最小化和缩放，需验证实现 |
| `.scrollWheel` 残留 | P1 | MenuBarController 第 603 行事件监视器匹配列表中残留 `.scrollWheel` |
| `cleanupFinishedEntry()` 死代码 | P2 | MenuBarController 第 310-315 行从未被调用 |
| SettingsContentView 索引越界风险 | P2 | `scrollViews` 数组只有 4 个元素，需验证 |

---

## 7. 骨架版本 (v0.1-skeleton) 定义

**标签:** `v0.1-skeleton`（打 tag 后永远不动）

**骨架包含的功能：**
1. ✅ 应用启动，菜单栏显示图标
2. ✅ 左键点击弹出 NSMenu（测试/关于/设置/退出）
3. ✅ 右键点击弹出上下文菜单
4. ✅ 测试菜单项弹出 Alert（验证事件链路）
5. ✅ Debug 模式 `.regular` / Release 模式 `.accessory`
6. ✅ 基础 TimerManager 初始化

**骨架不包含的功能：**
- ✅ 拖拽交互
- ✅ 悬停弹窗
- 🚫 倒计时浮窗（已废弃，改由 CompletionBanner 承接）
- ✅ 自绘完成弹窗 + 提示音
- ✅ 日历集成
- ✅ 设置窗口（5 tab 完整）

**验证标准：**
- Xcode ⌘R → 菜单栏图标出现 → 左键弹出菜单 → 点击"测试"弹出 Alert → 不卡死
- Archive → Release .app → 双击运行 → 隐藏 Dock 图标 → 行为一致

---

## 8. 颜色方案

```
强调色 Amber:     #F5A623
暖白:              #FFFFFF
柔灰:              #888888
暗色面板背景:       rgba(12, 12, 14, 0.92)
亮色面板背景:       rgba(242, 242, 245, 0.95)
毛玻璃:            backdrop-blur 16px
```

---

## 9. Roadmap — 开发阶段

### Phase 1: 骨架重建 ✅（v0.1-skeleton）
1. ✅ 新建 Xcode 项目 Linger2.xcodeproj
2. ✅ 搭建骨架：AppDelegate → MenuBarManager → NSMenu
3. ✅ 打 tag `v0.1-skeleton`
4. ✅ Archive 验证 Release 行为

### Phase 2: 核心逻辑移植 ✅
1. ✅ 移植 TimerManager + TimerEntry
2. ✅ 移植拖拽状态机（修复 trackingArea owner）
3. ✅ 拖拽反馈窗口（双轨布局、弹簧反馈）
4. ✅ 右键菜单
5. ⏳ 拆分 HoverListView 到独立文件（见 Phase 3）

### Phase 3: 暂停/继续 + 悬停列表（✅）
1. ✅ 暂停/继续（UI 入口完整：卡片右侧按钮 + 底栏全部暂停/继续）
2. ✅ 暂停全部/继续全部/清空全部
3. ✅ 悬停计时列表（HoverListView 完整实现：三组排序 + FLIP 动画 + 编辑 + 日历按钮入口）
4. ✅ 预约计时已实现（ScheduleTimerView：hover-list 内联展开 · 胶囊三行编辑区 · 到点自动激活 · 日历同步）

### Phase 4: 设置窗口 ✅
1. ✅ 操作设置（最大时长、计时字号、双轨模式选择器）
2. ✅ 通知设置（完成弹窗开关、提示音开关）
3. ✅ 日历设置（日历授权状态、目标日历、写入方式、默认标题、快捷键预设）
4. ✅ 通用设置（菜单栏图标 -> 已砍、启动登录、维护、导出记录）
5. ✅ 关于（白底票据面板）

### Phase 5: 通知系统 + 日历集成 ✅
1. ✅ 完成弹窗自绘玻璃横幅（替换系统通知，无需通知权限）
2. ✅ 提示音（NotificationManager 仅保留提示音）
3. ✅ 日历集成（CalendarManager + CalendarRecorder）
4. ✅ 预约删除同步删日历事件
5. ✅ 完成弹窗输入标题→更新已有日历事件标题
6. ✅ 设置→日历页授权状态同步刷新

### Phase 6: UI 打磨（✅ 完成）
1. ✅ 统一倒计时面板替代多浮窗（⚠️ 后续已废弃，改由通知横幅承接）
2. ✅ 呼吸灯 + 边框呼吸同相同向 + 动态启停
3. ✅ dismiss 防反弹
4. ✅ 排序分组 + 分隔线逻辑（灰色）
5. ✅ 标题编辑图标（return ↩︎ / done ✓ / tab ⇥）
6. ✅ 面板可拖动（Padding + 行本身）
7. ✅ 排序过渡动画
8. ✅ 默认标题 "-" / 编辑文字白色
9. ✅ Tab 切换不被拦截 + 勾勾持久
10. ✅ X 按钮压边框 + 完整圆 + 白色 + 无矩形阴影框
11. ✅ 关闭闪现修复
12. ✅ DragFeedbackView 竖线渐变 + 弹簧线宽克制
13. ✅ DragFeedbackView for/til 前缀 + 底对齐 + 浅色自适应 + 双边吸附
14. ✅ DragFeedbackView hover 时机检测（NSEvent.mouseLocation 轮询替代 NSTrackingArea）
15. ✅ HoverListView 暂停/继续/停止 + 三组排序 + 颜色分层
16. ✅ HoverListView 单击编辑 + 首次锁定
17. ✅ HoverListView FLIP 动画（卡片位移 + 窗口高度同步）
18. ✅ HoverListView 底栏拆分双按钮（日历 + 暂停全部）
19. ✅ DragFeedbackView 定时一致性（始终按时长吸附）
20. ✅ DragFeedbackView hover 对侧联动（左边变大亮 + 右边变小暗）
21. ✅ 旧 CountdownFloater 代码清理（已删除）


---

*本文档基于 Linger 1.0 源代码分析及 macOS 菜单栏应用开发铁律（v1.0）撰写。*
